class Schedule {
  final String title, description, intensity, frequency, duration, attachments;

  Schedule(
      {required this.title,
      required this.description,
      required this.intensity,
      required this.frequency,
      required this.duration,
      required this.attachments});
}
